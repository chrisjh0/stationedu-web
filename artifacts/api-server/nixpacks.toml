[phases.setup]
nixPkgs = ["nodejs_20"]
cmds = ["npm install -g pnpm@9"]

[phases.install]
cmds = ["pnpm install --ignore-scripts"]

[phases.build]
cmds = ["node ./build.mjs"]

[start]
cmd = "node --enable-source-maps ./dist/index.mjs"
