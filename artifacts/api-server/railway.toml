[build]
builder = "nixpacks"
buildCommand = "npm install -g pnpm && pnpm install --ignore-scripts && node ./build.mjs"

[deploy]
startCommand = "node --enable-source-maps ./dist/index.mjs"
restartPolicyType = "on_failure"
